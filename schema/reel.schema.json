{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/KirtiJha/reel/main/schema/reel.schema.json",
  "title": "Reel demo spec",
  "description": "A demos-as-code spec: what to drive, how to film it, and what to render. Generated from Reel's own validation schema — see https://github.com/KirtiJha/reel.",
  "type": "object",
  "properties": {
    "version": {
      "type": "number",
      "const": 1,
      "default": 1,
      "description": "Spec format version, so the grammar can evolve under semver."
    },
    "name": {
      "type": "string",
      "default": "Untitled demo",
      "description": "Shown on title cards and in the interactive build."
    },
    "url": {
      "type": "string",
      "default": "http://localhost:3000",
      "description": "Where the demo starts. Any URL the browser can reach, not just localhost."
    },
    "viewport": {
      "type": "object",
      "properties": {
        "width": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "default": 1280
        },
        "height": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "default": 800
        },
        "scale": {
          "type": "number",
          "exclusiveMinimum": 0,
          "maximum": 4,
          "default": 2,
          "description": "device pixel ratio — 2 = retina."
        }
      },
      "additionalProperties": false,
      "default": {},
      "description": "The browser window the demo is filmed in."
    },
    "theme": {
      "type": "string",
      "enum": [
        "light",
        "dark"
      ],
      "default": "light",
      "description": "The colour scheme the app is asked to render in (`prefers-color-scheme`)."
    },
    "run": {
      "type": "object",
      "properties": {
        "cmd": {
          "type": "string",
          "description": "Shell command to boot the app (e.g. \"npm run dev\")."
        },
        "cwd": {
          "type": "string",
          "description": "Working directory for the command."
        },
        "readyOn": {
          "type": "string",
          "description": "URL/port to poll until the app is ready before recording."
        },
        "timeout": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "default": 60000,
          "description": "Max ms to wait for readiness."
        },
        "env": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra env vars for the spawned process."
        }
      },
      "required": [
        "cmd"
      ],
      "additionalProperties": false,
      "description": "App lifecycle — how the app under test gets started. The plan implied a\nrunning `url` but never said how it boots; real demos need this."
    },
    "deterministic": {
      "type": "object",
      "properties": {
        "freezeClock": {
          "type": [
            "string",
            "number"
          ],
          "description": "Freeze Date/performance.now to a fixed instant. ISO string or epoch ms."
        },
        "disableAnimations": {
          "type": "boolean",
          "default": true,
          "description": "Disable CSS animations/transitions and the text caret blink."
        },
        "seedRandom": {
          "type": "number",
          "description": "Seed Math.random for reproducible app behaviour."
        },
        "reducedMotion": {
          "type": "boolean",
          "default": false,
          "description": "Force prefers-reduced-motion at the app level."
        },
        "locale": {
          "type": "string",
          "description": "Pin the browser locale (e.g. \"en-US\"). Without this, a CI runner with a\ndifferent system locale formats dates/numbers differently than your laptop\n— a frozen clock alone doesn't make text reproducible."
        },
        "timezone": {
          "type": "string",
          "description": "Pin the browser timezone (e.g. \"UTC\", \"America/New_York\")."
        },
        "timeline": {
          "type": "boolean",
          "default": true,
          "description": "Drive the recording off a virtual clock instead of wall-clock time, so the\nsame spec against the same app renders byte-identical media on any machine.\nWithout it, output length swings with CPU load and CI rewrites the media on\nevery push whether or not the demo changed.\n\nTurn it off only to film an app's own animation as it really happens."
        }
      },
      "additionalProperties": false,
      "default": {},
      "description": "Determinism controls. Real web apps are non-deterministic (animations,\nDate.now(), live data). VHS never had to care because a terminal is fully\ncontrolled — a browser is not. These make `reel check` stable and keep\ndrift-detection from crying wolf."
    },
    "polish": {
      "type": "object",
      "properties": {
        "zoom": {
          "type": [
            "string",
            "boolean"
          ],
          "enum": [
            "auto",
            false
          ],
          "default": "auto",
          "description": "auto = zoom toward the active element; false = never zoom."
        },
        "zoomOutput": {
          "type": "boolean",
          "default": false,
          "description": "In a terminal demo, let the camera follow each command's output — the same\nregion `zoom: { to: output }` names, applied after every command.\n\nOpt-in rather than implied by `zoom: auto`, which every spec gets by\ndefault: turning it on for everyone would re-shoot existing terminal demos\non upgrade, and `reel check` compares rendered output, so that lands as a\nCI failure rather than a nicer demo. `zoom: false` still switches off\neverything."
        },
        "zoomRows": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 120,
          "default": 12,
          "description": "Most rows the camera frames at once in a terminal demo. Longer output is\nframed at its tail, where the newest lines are."
        },
        "cursor": {
          "type": "string",
          "enum": [
            "smooth",
            "none"
          ],
          "default": "smooth",
          "description": "Render a synthetic cursor that eases between targets."
        },
        "captions": {
          "type": "boolean",
          "default": true,
          "description": "Show caption text overlays."
        },
        "frame": {
          "type": "string",
          "enum": [
            "none",
            "browser",
            "window"
          ],
          "default": "none",
          "description": "Device frame around the captured page:\n - none    : no chrome\n - browser : macOS-style window with traffic lights + a URL pill\n - window  : macOS-style window with traffic lights only"
        },
        "frameUrl": {
          "type": "string",
          "description": "Address shown in the browser frame's URL pill.\n\nDemos are filmed against whatever serves the app locally, so without this\nevery published video has `localhost:4500` in the chrome — which tells the\nviewer they are watching a developer's laptop rather than the product. Set\nit to the address people actually type.\n\nIt is cosmetic by design: it changes the pixels in the pill and nothing\nelse. The demo still runs against `url`, so this cannot be used to make a\nrecording claim it visited somewhere it did not."
        },
        "padding": {
          "type": "integer",
          "minimum": 0,
          "default": 0,
          "description": "Outer padding around the frame, in reference px (at 1000px-wide output;\nscales with resolution). When a frame is set, a sensible default is used."
        },
        "background": {
          "type": "string",
          "default": "#0b0b0f",
          "description": "Background behind the padded frame — a CSS color or gradient."
        },
        "radius": {
          "type": "integer",
          "minimum": 0,
          "default": 0,
          "description": "Rounded-corner radius (reference px) applied to the captured page."
        },
        "accent": {
          "type": "string",
          "minLength": 1,
          "default": "#6d8bff",
          "description": "Brand accent — used by the click ripple, the callout spotlight ring, and\nthe title-card rule. One knob so a repo's demos look like one product."
        },
        "speed": {
          "type": "number",
          "exclusiveMinimum": 0,
          "maximum": 10,
          "default": 1,
          "description": "Playback rate for every authored duration — holds, captions, typing, camera\nmoves. 2 renders the same demo in half the time; 0.5 slows it down. Real\nwaiting (selectors, network) is unaffected: this paces the demo, it doesn't\nrush the app."
        },
        "trimIdle": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "description": "Cap any stretch where nothing on screen changes at this many ms.\n\nBlunt on purpose, and worth understanding before reaching for it: it can't\ntell dead air from a deliberate pause, so a 1700ms title card under\n`trimIdle: 700` becomes 700ms — too fast to read. On a virtual timeline\nreal waiting already costs no demo time, so what's left to trim is mostly\nwhat you authored. Use it to rescue a demo full of long waits, not to\ntighten one that is already paced. To shorten a paced demo, prefer\n`speed` or `output.targetDuration`, which scale everything proportionally."
        },
        "idleMotion": {
          "type": "string",
          "enum": [
            "auto",
            "drift",
            "none"
          ],
          "default": "auto",
          "description": "Drift the camera where nothing on screen is changing.\n\nThe capture emits a frame only when something moves, so a stretch narrated\nover a settled screen is one still image held for however long the voice\ntakes. A slow push-in costs nothing — the frames are already on disk and\nthe camera is already interpolated — and it is the difference between a\ndemo and a slideshow.\n\n`auto` follows the camera: drift where `zoom: auto`, hold still where\n`zoom: false`. That default matters because `zoom: false` is usually a\nterminal demo, where pushing in would blur the text the demo exists to\nshow — and overriding that would be answering a question the author\nalready answered.\n\n`drift` asks for it regardless, which is the way to add life to a terminal\nchapter that would otherwise sit still under a long line of narration."
        },
        "idleMotionAfter": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "default": 1800,
          "description": "How long nothing may change before the camera starts to drift."
        },
        "fadeIn": {
          "type": "integer",
          "minimum": 0,
          "default": 0,
          "description": "Ramp the film up from `background` at the start, and back down at the end.\n\nWhat makes several chapters read as one piece. Reel's own tour is ten\nseparate renders joined with a stream copy — the copy is why the picture\nthat shipped is the picture that was verified — so a cross-fade *between*\nthe files would re-encode both sides of every join. A chapter that fades\nitself concatenates into a film that dissolves, at no cost."
        },
        "fadeOut": {
          "type": "integer",
          "minimum": 0,
          "default": 0
        },
        "idleMotionScale": {
          "type": "number",
          "minimum": 0.8,
          "maximum": 1,
          "default": 0.94,
          "description": "How far the drift pushes in, as a fraction of the shot. 0.94 is a 6% move\nacross the whole silence — noticeable as life, not as a zoom."
        }
      },
      "additionalProperties": false,
      "default": {},
      "description": "How the demo is filmed and presented: camera, cursor, captions, and the\nframe around the page. None of it changes what the app does."
    },
    "audio": {
      "type": "object",
      "properties": {
        "voice": {
          "type": "object",
          "properties": {
            "provider": {
              "type": "string",
              "enum": [
                "openai",
                "elevenlabs"
              ],
              "default": "openai"
            },
            "id": {
              "type": "string",
              "description": "Voice id or name, as the vendor names it. Omit for the provider default."
            },
            "model": {
              "type": "string",
              "description": "The synthesis model. Omit for the provider default."
            },
            "style": {
              "type": "string",
              "description": "Free-text direction — \"calm, confident technical explainer, unhurried\".\nHonoured where the vendor supports steering, and ignored with a warning\nwhere it doesn't, rather than silently changing nothing."
            },
            "speed": {
              "type": "number",
              "minimum": 0.5,
              "maximum": 2,
              "default": 1,
              "description": "Playback rate asked of the vendor. Past about 1.15 it starts to sound rushed."
            }
          },
          "additionalProperties": false,
          "default": {},
          "description": "The voice that reads the narration.\n\nThe API key is never written here. Specs are committed, often to public\nrepositories, which is the same reason there is deliberately no `${ENV}`\ninterpolation anywhere in this grammar — the key comes from the environment."
        },
        "fit": {
          "type": "string",
          "enum": [
            "flow",
            "stretch",
            "none"
          ],
          "default": "flow",
          "description": "What happens when a spoken line runs longer than the hold it was written\nfor — which it usually will, since `ms` was chosen for reading.\n\n`flow` is the default and what a product tour wants: the demo runs at the\npace it was authored, a line plays over whatever happens next, and time is\ninserted only where a line would collide with the next or run past the end.\n\n`stretch` grows the hold a line sits on until the sentence finishes. It\nnever cuts narration off and never lets the picture move either — a long\nline freezes the frame for as long as it takes to say. Right for a title\ncard being read aloud; wrong for anything with something to show.\n\n`none` leaves the timeline exactly as recorded and warns about every line\nthat overruns, for demos whose narration was written against known holds.\n\n(A fourth mode — keep the authored length and read faster to fit — is not\nhere yet. It needs re-synthesis at a computed rate, and a half-working\nversion that silently rushed the delivery would be worse than its absence.)"
        },
        "breathMs": {
          "type": "integer",
          "minimum": 0,
          "default": 350,
          "description": "Silence between consecutive spoken lines, so delivery has room to breathe."
        },
        "sfx": {
          "type": "string",
          "enum": [
            "none",
            "subtle",
            "full"
          ],
          "default": "none",
          "description": "UI sound design: a tick on a click, key texture while typing, a low sweep\nunder a title card.\n\nSynthesized, not sampled — there is nothing to license and nothing shipped.\nOff by default: it is a taste, and a demo that started making noises\nbecause narration was switched on would be a surprise rather than a\nfeature."
        },
        "music": {
          "type": "object",
          "properties": {
            "file": {
              "type": "string",
              "minLength": 1,
              "description": "Path to the audio file, relative to the spec. Looped to fit."
            },
            "gain": {
              "type": "number",
              "maximum": 0,
              "default": -22,
              "description": "Level before ducking, in dB. Beds belong well under the voice."
            },
            "duck": {
              "type": "number",
              "maximum": 0,
              "default": -12,
              "description": "How far the bed drops while someone is speaking, in dB.\n\nHonoured exactly: the envelope is built from the narration timings\nrather than from a compressor listening to the voice, so this number\nmeans what it says."
            },
            "fadeIn": {
              "type": "integer",
              "minimum": 0,
              "default": 1200,
              "description": "const used above its declaration is a runtime error, not a type error."
            },
            "fadeOut": {
              "type": "integer",
              "minimum": 0,
              "default": 2500
            }
          },
          "required": [
            "file"
          ],
          "additionalProperties": false,
          "description": "A music bed under the whole demo.\n\nReel ships no tracks. Licensing makes bundling them a liability, and a\nbundled bed would be instantly recognisable across everyone's demos — point\n`file` at something you have the right to use."
        }
      },
      "additionalProperties": false,
      "description": "Voice and timing for spoken narration. Top-level rather than under\n`polish:` because it is not a look — every cut of a recording shares it."
    },
    "storageState": {
      "type": "string",
      "description": "Playwright storageState path for logged-in demos."
    },
    "redact": {
      "type": "object",
      "properties": {
        "selectors": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "mode": {
          "type": "string",
          "enum": [
            "blur",
            "box"
          ],
          "default": "blur"
        },
        "blur": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "default": 12
        }
      },
      "additionalProperties": false,
      "description": "Blur/box sensitive regions so demos don't leak real data."
    },
    "mock": {
      "type": "object",
      "properties": {
        "har": {
          "type": "string"
        },
        "routes": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "url": {
                "type": "string"
              },
              "status": {
                "type": "integer",
                "default": 200
              },
              "body": {
                "type": "string"
              },
              "json": {},
              "contentType": {
                "type": "string"
              }
            },
            "required": [
              "url"
            ],
            "additionalProperties": false
          },
          "default": []
        }
      },
      "additionalProperties": false,
      "description": "Pin network responses (HAR replay / route stubs) for clean, stable data."
    },
    "retries": {
      "type": "integer",
      "minimum": 0,
      "maximum": 5,
      "default": 0,
      "description": "Retry a step that fails for a transient reason (an element not ready yet,\na slow response). Only steps that provably didn't act are retried — a\ntimed-out click never clicked — so a retry can't double-submit a form or\ntype the same text twice."
    },
    "matrix": {
      "type": "object",
      "properties": {
        "viewports": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "width": {
                "type": "integer",
                "exclusiveMinimum": 0,
                "default": 1280
              },
              "height": {
                "type": "integer",
                "exclusiveMinimum": 0,
                "default": 800
              },
              "scale": {
                "type": "number",
                "exclusiveMinimum": 0,
                "maximum": 4,
                "default": 2
              },
              "name": {
                "type": "string",
                "minLength": 1
              }
            },
            "required": [
              "name"
            ],
            "additionalProperties": false
          },
          "minItems": 1
        },
        "themes": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "light",
              "dark"
            ]
          },
          "minItems": 1
        }
      },
      "additionalProperties": false,
      "description": "Render this one spec at several viewports and/or themes."
    },
    "terminal": {
      "type": "object",
      "properties": {
        "cols": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 300,
          "default": 90
        },
        "rows": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 120,
          "default": 24
        },
        "cwd": {
          "type": "string",
          "description": "Working directory for commands; relative to the spec file."
        },
        "env": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "prompt": {
          "type": "string",
          "default": "$ ",
          "description": "The prompt drawn before each typed command."
        },
        "title": {
          "type": "string",
          "default": "zsh — reel"
        },
        "theme": {
          "type": "string",
          "enum": [
            "reel",
            "dracula",
            "nord",
            "catppuccin-mocha",
            "tokyo-night",
            "gruvbox-dark",
            "one-dark",
            "solarized-dark",
            "solarized-light",
            "github-light"
          ],
          "default": "reel",
          "description": "Named colour scheme. Supplies the 16 ANSI colours plus a matching\nbackground and foreground; set either of those explicitly to override it."
        },
        "background": {
          "type": "string",
          "minLength": 1
        },
        "foreground": {
          "type": "string",
          "minLength": 1
        },
        "palette": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "minItems": 16,
          "maxItems": 16,
          "description": "Replace the theme's 16 ANSI colours outright: 8 normal, then 8 bright."
        },
        "fontSize": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "default": 15
        },
        "fontFamily": {
          "type": "string"
        },
        "require": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "default": [],
          "description": "Programs the demo needs on PATH.\n\nChecked once before anything runs, so a missing dependency fails with the\nname of what's absent rather than partway through with whatever error the\nshell happened to produce."
        },
        "typing": {
          "type": "integer",
          "minimum": 0,
          "default": 55,
          "description": "Per-character delay while the command is typed on camera (ms)."
        },
        "timeout": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "default": 120000,
          "description": "Longest a single command may take before it's killed (ms)."
        },
        "replayMs": {
          "type": "integer",
          "minimum": 0,
          "default": 2200,
          "description": "Longest the captured output may take to stream back on camera (ms).\nOutput is replayed, not filmed live, so a slow command still reads fast."
        }
      },
      "additionalProperties": false,
      "description": "Enable terminal steps, and configure how the terminal looks and behaves."
    },
    "steps": {
      "type": "array",
      "items": {
        "anyOf": [
          {
            "anyOf": [
              {
                "type": "object",
                "properties": {
                  "goto": {
                    "type": "string"
                  }
                },
                "required": [
                  "goto"
                ],
                "additionalProperties": false,
                "description": "Navigate. A path is resolved against `url`; a full URL is used as given."
              },
              {
                "type": "object",
                "properties": {
                  "signIn": {
                    "anyOf": [
                      {
                        "type": "string"
                      },
                      {
                        "type": "object",
                        "properties": {
                          "state": {
                            "type": "string",
                            "description": "Path to a Playwright storage state, relative to the spec."
                          },
                          "goto": {
                            "type": "string",
                            "description": "Where to land afterwards. Omit to reload wherever the demo is."
                          }
                        },
                        "required": [
                          "state"
                        ],
                        "additionalProperties": false
                      }
                    ]
                  }
                },
                "required": [
                  "signIn"
                ],
                "additionalProperties": false,
                "description": "Become signed in, mid-demo, without filming the sign-in.\n\n`storageState` authenticates a whole run, which is the right answer when\nthe demo starts inside the product. It is the wrong answer when the demo\nopens on a marketing page, a logged-out home screen or a paywall — the\nstory needs to begin signed *out* and cross over.\n\nThis applies a session saved by `reel capture --save-auth` to the running\nbrowser and reloads, so one continuous demo can show the logged-out page,\ncover the transition with a card, and carry on inside the product. No\njump cut between two renders, one camera path, one caption timeline.\n\nDeliberately a saved session rather than a scripted login: filming a real\nsign-in means a password in the spec, and there is no way to put one there\nfor exactly that reason."
              },
              {
                "type": "object",
                "properties": {
                  "click": {
                    "type": "string",
                    "minLength": 1
                  }
                },
                "required": [
                  "click"
                ],
                "additionalProperties": false,
                "description": "Click an element. The cursor glides to it first, and the click ripples."
              },
              {
                "type": "object",
                "properties": {
                  "dblclick": {
                    "type": "string",
                    "minLength": 1
                  }
                },
                "required": [
                  "dblclick"
                ],
                "additionalProperties": false,
                "description": "Double-click an element."
              },
              {
                "type": "object",
                "properties": {
                  "drag": {
                    "type": "object",
                    "properties": {
                      "from": {
                        "type": "string",
                        "minLength": 1
                      },
                      "to": {
                        "anyOf": [
                          {
                            "type": "string",
                            "minLength": 1
                          },
                          {
                            "type": "object",
                            "properties": {
                              "x": {
                                "type": "number"
                              },
                              "y": {
                                "type": "number"
                              }
                            },
                            "required": [
                              "x",
                              "y"
                            ],
                            "additionalProperties": false
                          }
                        ],
                        "description": "The element to drop onto, or a viewport point."
                      },
                      "ms": {
                        "type": "integer",
                        "minimum": 0,
                        "default": 900,
                        "description": "How long the travel takes on camera."
                      }
                    },
                    "required": [
                      "from",
                      "to"
                    ],
                    "additionalProperties": false
                  }
                },
                "required": [
                  "drag"
                ],
                "additionalProperties": false,
                "description": "Drag one element onto another — a card between columns, a node onto a\ncanvas, a handle along a slider.\n\nThe gesture a click grammar cannot express, and the reason kanban boards,\nflow builders and range inputs could not be filmed at all. `to` is another\nelement, or a point when the destination is empty canvas with nothing to\nname."
              },
              {
                "type": "object",
                "properties": {
                  "hover": {
                    "type": "string",
                    "minLength": 1
                  }
                },
                "required": [
                  "hover"
                ],
                "additionalProperties": false,
                "description": "Move the cursor onto an element without clicking — for menus and tooltips."
              },
              {
                "type": "object",
                "properties": {
                  "type": {
                    "type": "object",
                    "properties": {
                      "selector": {
                        "type": "string",
                        "minLength": 1
                      },
                      "text": {
                        "type": "string"
                      },
                      "delay": {
                        "type": "integer",
                        "minimum": 0,
                        "default": 60,
                        "description": "Per-character delay (ms) so typing reads naturally on camera."
                      }
                    },
                    "required": [
                      "selector",
                      "text"
                    ],
                    "additionalProperties": false
                  }
                },
                "required": [
                  "type"
                ],
                "additionalProperties": false,
                "description": "Type into a field, character by character, so the typing reads on camera."
              },
              {
                "type": "object",
                "properties": {
                  "press": {
                    "type": "object",
                    "properties": {
                      "selector": {
                        "type": "string",
                        "minLength": 1
                      },
                      "key": {
                        "type": "string"
                      }
                    },
                    "required": [
                      "key"
                    ],
                    "additionalProperties": false
                  }
                },
                "required": [
                  "press"
                ],
                "additionalProperties": false,
                "description": "Press a key (`Enter`, `Escape`, `Control+K`). Targets an element, or the page."
              },
              {
                "type": "object",
                "properties": {
                  "fill": {
                    "type": "object",
                    "properties": {
                      "selector": {
                        "type": "string",
                        "minLength": 1
                      },
                      "text": {
                        "type": "string"
                      }
                    },
                    "required": [
                      "selector",
                      "text"
                    ],
                    "additionalProperties": false
                  }
                },
                "required": [
                  "fill"
                ],
                "additionalProperties": false,
                "description": "Set a field's value in one go. Use `type` when the typing is the point."
              },
              {
                "type": "object",
                "properties": {
                  "scrollTo": {
                    "type": "string",
                    "minLength": 1
                  }
                },
                "required": [
                  "scrollTo"
                ],
                "additionalProperties": false,
                "description": "Scroll an element into view. Use `scroll` for a filmed, cinematic pan."
              },
              {
                "type": "object",
                "properties": {
                  "waitFor": {
                    "anyOf": [
                      {
                        "type": "string",
                        "minLength": 1
                      },
                      {
                        "type": "object",
                        "properties": {
                          "selector": {
                            "type": "string",
                            "minLength": 1
                          },
                          "timeout": {
                            "type": "integer",
                            "exclusiveMinimum": 0
                          }
                        },
                        "required": [
                          "selector",
                          "timeout"
                        ],
                        "additionalProperties": false
                      }
                    ]
                  }
                },
                "required": [
                  "waitFor"
                ],
                "additionalProperties": false,
                "description": "State-based wait — the reliability moat. Never a raw sleep by default.\n\nThe object form exists for the rare wait that is legitimately long: a build\nrunning inside the page, a job whose progress the UI streams. The default\nis deliberately short so that a *broken* selector fails fast instead of\nstalling the recording, and raising it globally would trade that away for\nevery step. Naming the one step that needs longer keeps the rest honest."
              },
              {
                "type": "object",
                "properties": {
                  "waitForUrl": {
                    "type": "string"
                  }
                },
                "required": [
                  "waitForUrl"
                ],
                "additionalProperties": false,
                "description": "Wait until the address bar matches — a substring, or a glob with `*`."
              },
              {
                "type": "object",
                "properties": {
                  "waitForNetworkIdle": {
                    "type": "boolean",
                    "default": true
                  }
                },
                "additionalProperties": false,
                "description": "Wait for in-flight requests to settle. A last resort — prefer `waitFor`."
              },
              {
                "type": "object",
                "properties": {
                  "caption": {
                    "anyOf": [
                      {
                        "type": "string"
                      },
                      {
                        "type": "object",
                        "properties": {
                          "text": {
                            "type": "string"
                          },
                          "ms": {
                            "type": "integer",
                            "minimum": 0,
                            "description": "How long to hold this caption (ms). Omit to run until the next one."
                          },
                          "position": {
                            "type": "string",
                            "enum": [
                              "bottom",
                              "top"
                            ],
                            "default": "bottom"
                          },
                          "say": {
                            "anyOf": [
                              {
                                "type": "string",
                                "minLength": 1
                              },
                              {
                                "type": "boolean",
                                "const": false
                              }
                            ],
                            "description": "What the narrator says here, written for the ear rather than the eye.\nOmit and the caption's own text is spoken; `false` keeps this caption\nsilent, which is right for a label that reads better than it speaks."
                          },
                          "sayIn": {
                            "type": "object",
                            "additionalProperties": {
                              "type": "string",
                              "minLength": 1
                            },
                            "description": "The same line in other languages, for `output.languages`."
                          }
                        },
                        "required": [
                          "text"
                        ],
                        "additionalProperties": false
                      }
                    ]
                  }
                },
                "required": [
                  "caption"
                ],
                "additionalProperties": false,
                "description": "Caption overlay. A bare string shows until the next caption; the object\nform can set an explicit duration and screen position."
              },
              {
                "type": "object",
                "properties": {
                  "say": {
                    "anyOf": [
                      {
                        "type": "string"
                      },
                      {
                        "type": "object",
                        "properties": {
                          "text": {
                            "type": "string"
                          },
                          "ms": {
                            "type": "integer",
                            "minimum": 0,
                            "description": "Hold at least this long. The spoken line extends it when it needs to."
                          },
                          "sayIn": {
                            "type": "object",
                            "additionalProperties": {
                              "type": "string",
                              "minLength": 1
                            },
                            "description": "The same line in other languages, for `output.languages`."
                          }
                        },
                        "required": [
                          "text"
                        ],
                        "additionalProperties": false
                      }
                    ]
                  }
                },
                "required": [
                  "say"
                ],
                "additionalProperties": false,
                "description": "Narration with nothing on screen — a line that carries the demo between two\nthings the viewer can see. Holds like a caption does, and draws nothing."
              },
              {
                "type": "object",
                "properties": {
                  "beat": {
                    "type": [
                      "string",
                      "boolean"
                    ]
                  }
                },
                "required": [
                  "beat"
                ],
                "additionalProperties": false,
                "description": "Hero frame: hold longer here. Optional label for the storyboard."
              },
              {
                "type": "object",
                "properties": {
                  "card": {
                    "anyOf": [
                      {
                        "type": "string"
                      },
                      {
                        "type": "object",
                        "properties": {
                          "title": {
                            "type": "string"
                          },
                          "subtitle": {
                            "type": "string"
                          },
                          "ms": {
                            "type": "integer",
                            "minimum": 0,
                            "default": 1800
                          },
                          "say": {
                            "anyOf": [
                              {
                                "type": "string",
                                "minLength": 1
                              },
                              {
                                "type": "boolean",
                                "const": false
                              }
                            ],
                            "description": "What the narrator says over the card. Unlike a caption there is no\nsensible fallback — a title read aloud sounds like a title — so a\ncard is silent unless this says otherwise."
                          },
                          "sayIn": {
                            "type": "object",
                            "additionalProperties": {
                              "type": "string",
                              "minLength": 1
                            },
                            "description": "The same line in other languages, for `output.languages`."
                          }
                        },
                        "required": [
                          "title"
                        ],
                        "additionalProperties": false
                      }
                    ]
                  }
                },
                "required": [
                  "card"
                ],
                "additionalProperties": false,
                "description": "A full-screen title card — the scene grammar a continuous screen recording\nlacks. Use it to open, to separate chapters, and to close."
              },
              {
                "type": "object",
                "properties": {
                  "callout": {
                    "type": "object",
                    "properties": {
                      "selector": {
                        "type": "string",
                        "minLength": 1
                      },
                      "text": {
                        "type": "string"
                      },
                      "ms": {
                        "type": "integer",
                        "minimum": 0,
                        "default": 1500
                      }
                    },
                    "required": [
                      "selector"
                    ],
                    "additionalProperties": false
                  }
                },
                "required": [
                  "callout"
                ],
                "additionalProperties": false,
                "description": "Spotlight an element: everything else dims, an accent ring draws around it,\nand an optional label explains it. The camera eases toward it too."
              },
              {
                "type": "object",
                "properties": {
                  "image": {
                    "anyOf": [
                      {
                        "type": "string",
                        "minLength": 1,
                        "description": "Shorthand: just the path."
                      },
                      {
                        "type": "object",
                        "properties": {
                          "file": {
                            "type": "string",
                            "minLength": 1,
                            "description": "Path to the image, relative to the spec."
                          },
                          "as": {
                            "type": "string",
                            "enum": [
                              "full",
                              "inset",
                              "split"
                            ],
                            "default": "full",
                            "description": "`full` fills the frame, `inset` sits in a corner with the app still\nvisible behind it, `split` takes one half of the screen."
                          },
                          "corner": {
                            "type": "string",
                            "enum": [
                              "tl",
                              "tr",
                              "bl",
                              "br"
                            ],
                            "default": "br",
                            "description": "Which corner an `inset` sits in."
                          },
                          "alt": {
                            "type": "string",
                            "description": "Alt text, for the interactive build. Describe what it shows."
                          },
                          "ms": {
                            "type": "integer",
                            "minimum": 0,
                            "default": 2600
                          },
                          "say": {
                            "anyOf": [
                              {
                                "type": "string",
                                "minLength": 1
                              },
                              {
                                "type": "boolean",
                                "const": false
                              }
                            ],
                            "description": "What the narrator says over it."
                          },
                          "sayIn": {
                            "type": "object",
                            "additionalProperties": {
                              "type": "string",
                              "minLength": 1
                            },
                            "description": "The same line in other languages, for `output.languages`."
                          }
                        },
                        "required": [
                          "file"
                        ],
                        "additionalProperties": false
                      }
                    ]
                  }
                },
                "required": [
                  "image"
                ],
                "additionalProperties": false,
                "description": "Bring in something the app cannot show: a logo, an architecture diagram, a\nchart, a before-and-after.\n\nThe file is always local and always read from disk — a render never\nfetches. A network request would make the output depend on a server being\nup and on what it served that day, which breaks the byte-identical promise,\nand it would quietly pull someone else's artwork into a video you publish.\nStudio may download an asset while you are *editing*, into the spec's own\ndirectory, committed like any other input.\n\n`diagram:` is the same step with the picture written as text instead."
              },
              {
                "type": "object",
                "properties": {
                  "diagram": {
                    "anyOf": [
                      {
                        "type": "string",
                        "minLength": 1,
                        "description": "Shorthand: just the Mermaid source."
                      },
                      {
                        "type": "object",
                        "properties": {
                          "mermaid": {
                            "type": "string",
                            "minLength": 1,
                            "description": "The Mermaid source."
                          },
                          "as": {
                            "type": "string",
                            "enum": [
                              "full",
                              "inset",
                              "split"
                            ],
                            "default": "full"
                          },
                          "corner": {
                            "type": "string",
                            "enum": [
                              "tl",
                              "tr",
                              "bl",
                              "br"
                            ],
                            "default": "br"
                          },
                          "theme": {
                            "type": "string",
                            "enum": [
                              "dark",
                              "light"
                            ],
                            "description": "Light or dark styling. Defaults to the spec's own theme."
                          },
                          "alt": {
                            "type": "string"
                          },
                          "ms": {
                            "type": "integer",
                            "minimum": 0,
                            "default": 3000
                          },
                          "say": {
                            "anyOf": [
                              {
                                "type": "string",
                                "minLength": 1
                              },
                              {
                                "type": "boolean",
                                "const": false
                              }
                            ],
                            "description": "Narration for one step: what to say, or `false` to keep it silent."
                          },
                          "sayIn": {
                            "type": "object",
                            "additionalProperties": {
                              "type": "string",
                              "minLength": 1
                            },
                            "description": "The same line in other languages, keyed by code — `{ es: \"…\", ja: \"…\" }`.\n\nWritten by a person, because this is the copy a customer hears. Machine\ntranslation fills the gaps when a model is configured, but a demo is\nmarketing, and marketing copy nobody has read is not something to ship by\ndefault."
                          }
                        },
                        "required": [
                          "mermaid"
                        ],
                        "additionalProperties": false
                      }
                    ]
                  }
                },
                "required": [
                  "diagram"
                ],
                "additionalProperties": false,
                "description": "A diagram written as text: a Mermaid definition, rendered to an image and\ncached by content hash.\n\nThe point of writing it here rather than committing a PNG is that it diffs.\nA flowchart that changes in a pull request shows up as changed *words*, and\nthe picture is a build product like any other. The render is cached in\n`.reel-cache/diagram` and committed, so a checkout — or CI — draws the\ndiagram without a browser or a network."
              },
              {
                "type": "object",
                "properties": {
                  "transition": {
                    "anyOf": [
                      {
                        "type": "integer",
                        "minimum": 0,
                        "description": "Shorthand: how long the dip takes."
                      },
                      {
                        "type": "object",
                        "properties": {
                          "kind": {
                            "type": "string",
                            "const": "fade",
                            "default": "fade"
                          },
                          "ms": {
                            "type": "integer",
                            "minimum": 0,
                            "default": 500
                          },
                          "color": {
                            "type": "string",
                            "minLength": 1,
                            "description": "What it dips to. Defaults to the spec's own background."
                          }
                        },
                        "additionalProperties": false
                      }
                    ]
                  }
                },
                "required": [
                  "transition"
                ],
                "additionalProperties": false,
                "description": "Dip the picture to a colour and back — a soft cut.\n\n`fade` is the one kind implemented, and the only one a single recording can\nexpress: a wipe or a push needs two shots to move between, and a demo is\none continuous stream. Joining two *finished* films that way is a different\njob, and one that would re-encode both."
              },
              {
                "type": "object",
                "properties": {
                  "highlight": {
                    "type": "object",
                    "properties": {
                      "selector": {
                        "type": "string",
                        "minLength": 1
                      },
                      "shape": {
                        "type": "string",
                        "enum": [
                          "box",
                          "circle",
                          "underline"
                        ],
                        "default": "box",
                        "description": "`box` frames it, `circle` rings it, `underline` sits beneath it."
                      },
                      "style": {
                        "type": "string",
                        "enum": [
                          "drawn",
                          "clean"
                        ],
                        "default": "drawn",
                        "description": "`drawn` looks sketched by hand; `clean` is a precise stroke."
                      },
                      "label": {
                        "type": "string",
                        "description": "A few words beside the mark. Long text wants a `callout` instead."
                      },
                      "ms": {
                        "type": "integer",
                        "minimum": 0,
                        "default": 2600,
                        "description": "How long it stays up. Ignored when `until` names a beat."
                      },
                      "until": {
                        "type": "string",
                        "minLength": 1,
                        "description": "Keep it up until this beat, however many steps away that is — the\nreason a highlight is a span and not a hold. A beat that never happens\nleaves the mark up until the demo ends."
                      }
                    },
                    "required": [
                      "selector"
                    ],
                    "additionalProperties": false
                  }
                },
                "required": [
                  "highlight"
                ],
                "additionalProperties": false,
                "description": "Mark an element without interrupting: the rest of the screen is untouched,\nthe camera does not move, and the demo keeps running underneath.\n\nThe counterpart to `callout`, not a replacement for it. A callout says\n*stop and look at this* — it dims the page and holds the film. A highlight\nsays *and notice this, as we go*, which is what a demo narrated over a\nworking app needs. Use both; they are different sentences.\n\nSeveral may be on screen at once, and one can outlive the step that drew it\n— see `until`."
              },
              {
                "type": "object",
                "properties": {
                  "scroll": {
                    "type": "object",
                    "properties": {
                      "to": {
                        "anyOf": [
                          {
                            "type": "string",
                            "minLength": 1
                          },
                          {
                            "type": "number"
                          }
                        ]
                      },
                      "ms": {
                        "type": "integer",
                        "minimum": 0,
                        "default": 900
                      }
                    },
                    "required": [
                      "to"
                    ],
                    "additionalProperties": false
                  }
                },
                "required": [
                  "scroll"
                ],
                "additionalProperties": false,
                "description": "Cinematic eased scroll — to an element, or to an absolute Y offset."
              },
              {
                "type": "object",
                "properties": {
                  "zoom": {
                    "anyOf": [
                      {
                        "type": "string",
                        "const": "out"
                      },
                      {
                        "type": "object",
                        "properties": {
                          "to": {
                            "anyOf": [
                              {
                                "type": "string",
                                "const": "output"
                              },
                              {
                                "type": "string",
                                "const": "cursor"
                              },
                              {
                                "type": "string",
                                "minLength": 1
                              }
                            ],
                            "description": "A selector, or — in a terminal demo — `output` for the rows the last\ncommand wrote, `cursor` for the live prompt line, or `text=…` to\nframe whatever rows contain that text."
                          },
                          "level": {
                            "type": "number",
                            "minimum": 1,
                            "maximum": 4,
                            "description": "Magnification: 1 = whole viewport, 2 = half of it, …"
                          },
                          "ms": {
                            "type": "integer",
                            "minimum": 0,
                            "description": "Camera move duration (ms)."
                          }
                        },
                        "additionalProperties": false
                      }
                    ]
                  }
                },
                "required": [
                  "zoom"
                ],
                "additionalProperties": false,
                "description": "Explicit camera control, for when auto-zoom's choice isn't the story you're\ntelling. \"out\" is a wide establishing shot."
              },
              {
                "type": "object",
                "properties": {
                  "expect": {
                    "type": "object",
                    "properties": {
                      "selector": {
                        "type": "string",
                        "minLength": 1
                      },
                      "text": {
                        "type": "string",
                        "description": "The element's text must contain this."
                      },
                      "count": {
                        "type": "integer",
                        "minimum": 0,
                        "description": "Exactly this many elements must match."
                      },
                      "visible": {
                        "type": "boolean",
                        "default": true,
                        "description": "Whether the element must be visible (default true)."
                      }
                    },
                    "required": [
                      "selector"
                    ],
                    "additionalProperties": false
                  }
                },
                "required": [
                  "expect"
                ],
                "additionalProperties": false,
                "description": "An assertion. Unlike `waitFor` (which only proves an element appears), this\nchecks what the app actually rendered — so `reel check` is a real smoke\ntest, not just a selector-existence probe."
              },
              {
                "type": "object",
                "properties": {
                  "hold": {
                    "type": "integer",
                    "minimum": 0
                  }
                },
                "required": [
                  "hold"
                ],
                "additionalProperties": false,
                "description": "Explicit pause — discouraged, but sometimes you want a deliberate hold."
              },
              {
                "type": "object",
                "properties": {
                  "run": {
                    "anyOf": [
                      {
                        "type": "string"
                      },
                      {
                        "type": "object",
                        "properties": {
                          "cmd": {
                            "type": "string"
                          },
                          "input": {
                            "type": "string",
                            "description": "Text piped to stdin, for commands that prompt."
                          },
                          "expectCode": {
                            "type": "integer",
                            "description": "Fail the run unless the command exits with this code."
                          },
                          "replayMs": {
                            "type": "integer",
                            "minimum": 0,
                            "description": "Override the replay budget for this command (ms)."
                          },
                          "hidden": {
                            "type": "boolean",
                            "default": false,
                            "description": "Run the command off camera.\n\nFor the setup a demo needs but nobody wants to watch — seeding a\nfixture, creating a scratch directory, installing something. The\ncommand still runs and `expectCode` still applies, so it stays part of\nwhat `reel check` verifies; it simply never reaches the screen.\n\nNote that each command runs in its own shell, so `cd` in a hidden step\ndoes not carry into later ones. Set `terminal.cwd` for that."
                          }
                        },
                        "required": [
                          "cmd"
                        ],
                        "additionalProperties": false
                      }
                    ]
                  }
                },
                "required": [
                  "run"
                ],
                "additionalProperties": false,
                "description": "Type a command at the prompt, run it, and replay its output. The command\nreally runs — so like `expect`, this makes `reel check` a genuine smoke\ntest of the CLI, not a scripted illusion of one."
              },
              {
                "type": "object",
                "properties": {
                  "expectOutput": {
                    "type": "string"
                  }
                },
                "required": [
                  "expectOutput"
                ],
                "additionalProperties": false,
                "description": "Assert the terminal screen contains this text."
              },
              {
                "type": "object",
                "properties": {
                  "clear": {
                    "type": "boolean"
                  }
                },
                "required": [
                  "clear"
                ],
                "additionalProperties": false,
                "description": "Clear the terminal screen."
              },
              {
                "type": "object",
                "properties": {
                  "show": {
                    "type": "string",
                    "enum": [
                      "terminal",
                      "app"
                    ]
                  }
                },
                "required": [
                  "show"
                ],
                "additionalProperties": false,
                "description": "Switch between the terminal and the app underneath it."
              }
            ]
          },
          {
            "type": "object",
            "properties": {
              "branch": {
                "type": "object",
                "properties": {
                  "prompt": {
                    "type": "string",
                    "default": "Choose a path",
                    "description": "The question put to the viewer, e.g. \"What do you want to see?\""
                  },
                  "paths": {
                    "type": "array",
                    "items": {
                      "type": "object",
                      "properties": {
                        "label": {
                          "type": "string",
                          "minLength": 1
                        },
                        "default": {
                          "type": "boolean",
                          "default": false,
                          "description": "The path the video follows, and the one pre-selected in the click-through.\nDefaults to the first path when none is marked."
                        },
                        "steps": {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "object",
                                "properties": {
                                  "goto": {
                                    "type": "string"
                                  }
                                },
                                "required": [
                                  "goto"
                                ],
                                "additionalProperties": false,
                                "description": "Navigate. A path is resolved against `url`; a full URL is used as given."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "signIn": {
                                    "anyOf": [
                                      {
                                        "type": "string"
                                      },
                                      {
                                        "type": "object",
                                        "properties": {
                                          "state": {
                                            "type": "string",
                                            "description": "Path to a Playwright storage state, relative to the spec."
                                          },
                                          "goto": {
                                            "type": "string",
                                            "description": "Where to land afterwards. Omit to reload wherever the demo is."
                                          }
                                        },
                                        "required": [
                                          "state"
                                        ],
                                        "additionalProperties": false
                                      }
                                    ]
                                  }
                                },
                                "required": [
                                  "signIn"
                                ],
                                "additionalProperties": false,
                                "description": "Become signed in, mid-demo, without filming the sign-in.\n\n`storageState` authenticates a whole run, which is the right answer when\nthe demo starts inside the product. It is the wrong answer when the demo\nopens on a marketing page, a logged-out home screen or a paywall — the\nstory needs to begin signed *out* and cross over.\n\nThis applies a session saved by `reel capture --save-auth` to the running\nbrowser and reloads, so one continuous demo can show the logged-out page,\ncover the transition with a card, and carry on inside the product. No\njump cut between two renders, one camera path, one caption timeline.\n\nDeliberately a saved session rather than a scripted login: filming a real\nsign-in means a password in the spec, and there is no way to put one there\nfor exactly that reason."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "click": {
                                    "type": "string",
                                    "minLength": 1
                                  }
                                },
                                "required": [
                                  "click"
                                ],
                                "additionalProperties": false,
                                "description": "Click an element. The cursor glides to it first, and the click ripples."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "dblclick": {
                                    "type": "string",
                                    "minLength": 1
                                  }
                                },
                                "required": [
                                  "dblclick"
                                ],
                                "additionalProperties": false,
                                "description": "Double-click an element."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "drag": {
                                    "type": "object",
                                    "properties": {
                                      "from": {
                                        "type": "string",
                                        "minLength": 1
                                      },
                                      "to": {
                                        "anyOf": [
                                          {
                                            "type": "string",
                                            "minLength": 1
                                          },
                                          {
                                            "type": "object",
                                            "properties": {
                                              "x": {
                                                "type": "number"
                                              },
                                              "y": {
                                                "type": "number"
                                              }
                                            },
                                            "required": [
                                              "x",
                                              "y"
                                            ],
                                            "additionalProperties": false
                                          }
                                        ],
                                        "description": "The element to drop onto, or a viewport point."
                                      },
                                      "ms": {
                                        "type": "integer",
                                        "minimum": 0,
                                        "default": 900,
                                        "description": "How long the travel takes on camera."
                                      }
                                    },
                                    "required": [
                                      "from",
                                      "to"
                                    ],
                                    "additionalProperties": false
                                  }
                                },
                                "required": [
                                  "drag"
                                ],
                                "additionalProperties": false,
                                "description": "Drag one element onto another — a card between columns, a node onto a\ncanvas, a handle along a slider.\n\nThe gesture a click grammar cannot express, and the reason kanban boards,\nflow builders and range inputs could not be filmed at all. `to` is another\nelement, or a point when the destination is empty canvas with nothing to\nname."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "hover": {
                                    "type": "string",
                                    "minLength": 1
                                  }
                                },
                                "required": [
                                  "hover"
                                ],
                                "additionalProperties": false,
                                "description": "Move the cursor onto an element without clicking — for menus and tooltips."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "type": {
                                    "type": "object",
                                    "properties": {
                                      "selector": {
                                        "type": "string",
                                        "minLength": 1
                                      },
                                      "text": {
                                        "type": "string"
                                      },
                                      "delay": {
                                        "type": "integer",
                                        "minimum": 0,
                                        "default": 60,
                                        "description": "Per-character delay (ms) so typing reads naturally on camera."
                                      }
                                    },
                                    "required": [
                                      "selector",
                                      "text"
                                    ],
                                    "additionalProperties": false
                                  }
                                },
                                "required": [
                                  "type"
                                ],
                                "additionalProperties": false,
                                "description": "Type into a field, character by character, so the typing reads on camera."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "press": {
                                    "type": "object",
                                    "properties": {
                                      "selector": {
                                        "type": "string",
                                        "minLength": 1
                                      },
                                      "key": {
                                        "type": "string"
                                      }
                                    },
                                    "required": [
                                      "key"
                                    ],
                                    "additionalProperties": false
                                  }
                                },
                                "required": [
                                  "press"
                                ],
                                "additionalProperties": false,
                                "description": "Press a key (`Enter`, `Escape`, `Control+K`). Targets an element, or the page."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "fill": {
                                    "type": "object",
                                    "properties": {
                                      "selector": {
                                        "type": "string",
                                        "minLength": 1
                                      },
                                      "text": {
                                        "type": "string"
                                      }
                                    },
                                    "required": [
                                      "selector",
                                      "text"
                                    ],
                                    "additionalProperties": false
                                  }
                                },
                                "required": [
                                  "fill"
                                ],
                                "additionalProperties": false,
                                "description": "Set a field's value in one go. Use `type` when the typing is the point."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "scrollTo": {
                                    "type": "string",
                                    "minLength": 1
                                  }
                                },
                                "required": [
                                  "scrollTo"
                                ],
                                "additionalProperties": false,
                                "description": "Scroll an element into view. Use `scroll` for a filmed, cinematic pan."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "waitFor": {
                                    "anyOf": [
                                      {
                                        "type": "string",
                                        "minLength": 1
                                      },
                                      {
                                        "type": "object",
                                        "properties": {
                                          "selector": {
                                            "type": "string",
                                            "minLength": 1
                                          },
                                          "timeout": {
                                            "type": "integer",
                                            "exclusiveMinimum": 0
                                          }
                                        },
                                        "required": [
                                          "selector",
                                          "timeout"
                                        ],
                                        "additionalProperties": false
                                      }
                                    ]
                                  }
                                },
                                "required": [
                                  "waitFor"
                                ],
                                "additionalProperties": false,
                                "description": "State-based wait — the reliability moat. Never a raw sleep by default.\n\nThe object form exists for the rare wait that is legitimately long: a build\nrunning inside the page, a job whose progress the UI streams. The default\nis deliberately short so that a *broken* selector fails fast instead of\nstalling the recording, and raising it globally would trade that away for\nevery step. Naming the one step that needs longer keeps the rest honest."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "waitForUrl": {
                                    "type": "string"
                                  }
                                },
                                "required": [
                                  "waitForUrl"
                                ],
                                "additionalProperties": false,
                                "description": "Wait until the address bar matches — a substring, or a glob with `*`."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "waitForNetworkIdle": {
                                    "type": "boolean",
                                    "default": true
                                  }
                                },
                                "additionalProperties": false,
                                "description": "Wait for in-flight requests to settle. A last resort — prefer `waitFor`."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "caption": {
                                    "anyOf": [
                                      {
                                        "type": "string"
                                      },
                                      {
                                        "type": "object",
                                        "properties": {
                                          "text": {
                                            "type": "string"
                                          },
                                          "ms": {
                                            "type": "integer",
                                            "minimum": 0,
                                            "description": "How long to hold this caption (ms). Omit to run until the next one."
                                          },
                                          "position": {
                                            "type": "string",
                                            "enum": [
                                              "bottom",
                                              "top"
                                            ],
                                            "default": "bottom"
                                          },
                                          "say": {
                                            "anyOf": [
                                              {
                                                "type": "string",
                                                "minLength": 1
                                              },
                                              {
                                                "type": "boolean",
                                                "const": false
                                              }
                                            ],
                                            "description": "What the narrator says here, written for the ear rather than the eye.\nOmit and the caption's own text is spoken; `false` keeps this caption\nsilent, which is right for a label that reads better than it speaks."
                                          },
                                          "sayIn": {
                                            "type": "object",
                                            "additionalProperties": {
                                              "type": "string",
                                              "minLength": 1
                                            },
                                            "description": "The same line in other languages, for `output.languages`."
                                          }
                                        },
                                        "required": [
                                          "text"
                                        ],
                                        "additionalProperties": false
                                      }
                                    ]
                                  }
                                },
                                "required": [
                                  "caption"
                                ],
                                "additionalProperties": false,
                                "description": "Caption overlay. A bare string shows until the next caption; the object\nform can set an explicit duration and screen position."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "say": {
                                    "anyOf": [
                                      {
                                        "type": "string"
                                      },
                                      {
                                        "type": "object",
                                        "properties": {
                                          "text": {
                                            "type": "string"
                                          },
                                          "ms": {
                                            "type": "integer",
                                            "minimum": 0,
                                            "description": "Hold at least this long. The spoken line extends it when it needs to."
                                          },
                                          "sayIn": {
                                            "type": "object",
                                            "additionalProperties": {
                                              "type": "string",
                                              "minLength": 1
                                            },
                                            "description": "The same line in other languages, for `output.languages`."
                                          }
                                        },
                                        "required": [
                                          "text"
                                        ],
                                        "additionalProperties": false
                                      }
                                    ]
                                  }
                                },
                                "required": [
                                  "say"
                                ],
                                "additionalProperties": false,
                                "description": "Narration with nothing on screen — a line that carries the demo between two\nthings the viewer can see. Holds like a caption does, and draws nothing."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "beat": {
                                    "type": [
                                      "string",
                                      "boolean"
                                    ]
                                  }
                                },
                                "required": [
                                  "beat"
                                ],
                                "additionalProperties": false,
                                "description": "Hero frame: hold longer here. Optional label for the storyboard."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "card": {
                                    "anyOf": [
                                      {
                                        "type": "string"
                                      },
                                      {
                                        "type": "object",
                                        "properties": {
                                          "title": {
                                            "type": "string"
                                          },
                                          "subtitle": {
                                            "type": "string"
                                          },
                                          "ms": {
                                            "type": "integer",
                                            "minimum": 0,
                                            "default": 1800
                                          },
                                          "say": {
                                            "anyOf": [
                                              {
                                                "type": "string",
                                                "minLength": 1
                                              },
                                              {
                                                "type": "boolean",
                                                "const": false
                                              }
                                            ],
                                            "description": "What the narrator says over the card. Unlike a caption there is no\nsensible fallback — a title read aloud sounds like a title — so a\ncard is silent unless this says otherwise."
                                          },
                                          "sayIn": {
                                            "type": "object",
                                            "additionalProperties": {
                                              "type": "string",
                                              "minLength": 1
                                            },
                                            "description": "The same line in other languages, for `output.languages`."
                                          }
                                        },
                                        "required": [
                                          "title"
                                        ],
                                        "additionalProperties": false
                                      }
                                    ]
                                  }
                                },
                                "required": [
                                  "card"
                                ],
                                "additionalProperties": false,
                                "description": "A full-screen title card — the scene grammar a continuous screen recording\nlacks. Use it to open, to separate chapters, and to close."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "callout": {
                                    "type": "object",
                                    "properties": {
                                      "selector": {
                                        "type": "string",
                                        "minLength": 1
                                      },
                                      "text": {
                                        "type": "string"
                                      },
                                      "ms": {
                                        "type": "integer",
                                        "minimum": 0,
                                        "default": 1500
                                      }
                                    },
                                    "required": [
                                      "selector"
                                    ],
                                    "additionalProperties": false
                                  }
                                },
                                "required": [
                                  "callout"
                                ],
                                "additionalProperties": false,
                                "description": "Spotlight an element: everything else dims, an accent ring draws around it,\nand an optional label explains it. The camera eases toward it too."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "image": {
                                    "anyOf": [
                                      {
                                        "type": "string",
                                        "minLength": 1,
                                        "description": "Shorthand: just the path."
                                      },
                                      {
                                        "type": "object",
                                        "properties": {
                                          "file": {
                                            "type": "string",
                                            "minLength": 1,
                                            "description": "Path to the image, relative to the spec."
                                          },
                                          "as": {
                                            "type": "string",
                                            "enum": [
                                              "full",
                                              "inset",
                                              "split"
                                            ],
                                            "default": "full",
                                            "description": "`full` fills the frame, `inset` sits in a corner with the app still\nvisible behind it, `split` takes one half of the screen."
                                          },
                                          "corner": {
                                            "type": "string",
                                            "enum": [
                                              "tl",
                                              "tr",
                                              "bl",
                                              "br"
                                            ],
                                            "default": "br",
                                            "description": "Which corner an `inset` sits in."
                                          },
                                          "alt": {
                                            "type": "string",
                                            "description": "Alt text, for the interactive build. Describe what it shows."
                                          },
                                          "ms": {
                                            "type": "integer",
                                            "minimum": 0,
                                            "default": 2600
                                          },
                                          "say": {
                                            "anyOf": [
                                              {
                                                "type": "string",
                                                "minLength": 1
                                              },
                                              {
                                                "type": "boolean",
                                                "const": false
                                              }
                                            ],
                                            "description": "What the narrator says over it."
                                          },
                                          "sayIn": {
                                            "type": "object",
                                            "additionalProperties": {
                                              "type": "string",
                                              "minLength": 1
                                            },
                                            "description": "The same line in other languages, for `output.languages`."
                                          }
                                        },
                                        "required": [
                                          "file"
                                        ],
                                        "additionalProperties": false
                                      }
                                    ]
                                  }
                                },
                                "required": [
                                  "image"
                                ],
                                "additionalProperties": false,
                                "description": "Bring in something the app cannot show: a logo, an architecture diagram, a\nchart, a before-and-after.\n\nThe file is always local and always read from disk — a render never\nfetches. A network request would make the output depend on a server being\nup and on what it served that day, which breaks the byte-identical promise,\nand it would quietly pull someone else's artwork into a video you publish.\nStudio may download an asset while you are *editing*, into the spec's own\ndirectory, committed like any other input.\n\n`diagram:` is the same step with the picture written as text instead."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "diagram": {
                                    "anyOf": [
                                      {
                                        "type": "string",
                                        "minLength": 1,
                                        "description": "Shorthand: just the Mermaid source."
                                      },
                                      {
                                        "type": "object",
                                        "properties": {
                                          "mermaid": {
                                            "type": "string",
                                            "minLength": 1,
                                            "description": "The Mermaid source."
                                          },
                                          "as": {
                                            "type": "string",
                                            "enum": [
                                              "full",
                                              "inset",
                                              "split"
                                            ],
                                            "default": "full"
                                          },
                                          "corner": {
                                            "type": "string",
                                            "enum": [
                                              "tl",
                                              "tr",
                                              "bl",
                                              "br"
                                            ],
                                            "default": "br"
                                          },
                                          "theme": {
                                            "type": "string",
                                            "enum": [
                                              "dark",
                                              "light"
                                            ],
                                            "description": "Light or dark styling. Defaults to the spec's own theme."
                                          },
                                          "alt": {
                                            "type": "string"
                                          },
                                          "ms": {
                                            "type": "integer",
                                            "minimum": 0,
                                            "default": 3000
                                          },
                                          "say": {
                                            "anyOf": [
                                              {
                                                "type": "string",
                                                "minLength": 1
                                              },
                                              {
                                                "type": "boolean",
                                                "const": false
                                              }
                                            ],
                                            "description": "Narration for one step: what to say, or `false` to keep it silent."
                                          },
                                          "sayIn": {
                                            "type": "object",
                                            "additionalProperties": {
                                              "type": "string",
                                              "minLength": 1
                                            },
                                            "description": "The same line in other languages, keyed by code — `{ es: \"…\", ja: \"…\" }`.\n\nWritten by a person, because this is the copy a customer hears. Machine\ntranslation fills the gaps when a model is configured, but a demo is\nmarketing, and marketing copy nobody has read is not something to ship by\ndefault."
                                          }
                                        },
                                        "required": [
                                          "mermaid"
                                        ],
                                        "additionalProperties": false
                                      }
                                    ]
                                  }
                                },
                                "required": [
                                  "diagram"
                                ],
                                "additionalProperties": false,
                                "description": "A diagram written as text: a Mermaid definition, rendered to an image and\ncached by content hash.\n\nThe point of writing it here rather than committing a PNG is that it diffs.\nA flowchart that changes in a pull request shows up as changed *words*, and\nthe picture is a build product like any other. The render is cached in\n`.reel-cache/diagram` and committed, so a checkout — or CI — draws the\ndiagram without a browser or a network."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "transition": {
                                    "anyOf": [
                                      {
                                        "type": "integer",
                                        "minimum": 0,
                                        "description": "Shorthand: how long the dip takes."
                                      },
                                      {
                                        "type": "object",
                                        "properties": {
                                          "kind": {
                                            "type": "string",
                                            "const": "fade",
                                            "default": "fade"
                                          },
                                          "ms": {
                                            "type": "integer",
                                            "minimum": 0,
                                            "default": 500
                                          },
                                          "color": {
                                            "type": "string",
                                            "minLength": 1,
                                            "description": "What it dips to. Defaults to the spec's own background."
                                          }
                                        },
                                        "additionalProperties": false
                                      }
                                    ]
                                  }
                                },
                                "required": [
                                  "transition"
                                ],
                                "additionalProperties": false,
                                "description": "Dip the picture to a colour and back — a soft cut.\n\n`fade` is the one kind implemented, and the only one a single recording can\nexpress: a wipe or a push needs two shots to move between, and a demo is\none continuous stream. Joining two *finished* films that way is a different\njob, and one that would re-encode both."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "highlight": {
                                    "type": "object",
                                    "properties": {
                                      "selector": {
                                        "type": "string",
                                        "minLength": 1
                                      },
                                      "shape": {
                                        "type": "string",
                                        "enum": [
                                          "box",
                                          "circle",
                                          "underline"
                                        ],
                                        "default": "box",
                                        "description": "`box` frames it, `circle` rings it, `underline` sits beneath it."
                                      },
                                      "style": {
                                        "type": "string",
                                        "enum": [
                                          "drawn",
                                          "clean"
                                        ],
                                        "default": "drawn",
                                        "description": "`drawn` looks sketched by hand; `clean` is a precise stroke."
                                      },
                                      "label": {
                                        "type": "string",
                                        "description": "A few words beside the mark. Long text wants a `callout` instead."
                                      },
                                      "ms": {
                                        "type": "integer",
                                        "minimum": 0,
                                        "default": 2600,
                                        "description": "How long it stays up. Ignored when `until` names a beat."
                                      },
                                      "until": {
                                        "type": "string",
                                        "minLength": 1,
                                        "description": "Keep it up until this beat, however many steps away that is — the\nreason a highlight is a span and not a hold. A beat that never happens\nleaves the mark up until the demo ends."
                                      }
                                    },
                                    "required": [
                                      "selector"
                                    ],
                                    "additionalProperties": false
                                  }
                                },
                                "required": [
                                  "highlight"
                                ],
                                "additionalProperties": false,
                                "description": "Mark an element without interrupting: the rest of the screen is untouched,\nthe camera does not move, and the demo keeps running underneath.\n\nThe counterpart to `callout`, not a replacement for it. A callout says\n*stop and look at this* — it dims the page and holds the film. A highlight\nsays *and notice this, as we go*, which is what a demo narrated over a\nworking app needs. Use both; they are different sentences.\n\nSeveral may be on screen at once, and one can outlive the step that drew it\n— see `until`."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "scroll": {
                                    "type": "object",
                                    "properties": {
                                      "to": {
                                        "anyOf": [
                                          {
                                            "type": "string",
                                            "minLength": 1
                                          },
                                          {
                                            "type": "number"
                                          }
                                        ]
                                      },
                                      "ms": {
                                        "type": "integer",
                                        "minimum": 0,
                                        "default": 900
                                      }
                                    },
                                    "required": [
                                      "to"
                                    ],
                                    "additionalProperties": false
                                  }
                                },
                                "required": [
                                  "scroll"
                                ],
                                "additionalProperties": false,
                                "description": "Cinematic eased scroll — to an element, or to an absolute Y offset."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "zoom": {
                                    "anyOf": [
                                      {
                                        "type": "string",
                                        "const": "out"
                                      },
                                      {
                                        "type": "object",
                                        "properties": {
                                          "to": {
                                            "anyOf": [
                                              {
                                                "type": "string",
                                                "const": "output"
                                              },
                                              {
                                                "type": "string",
                                                "const": "cursor"
                                              },
                                              {
                                                "type": "string",
                                                "minLength": 1
                                              }
                                            ],
                                            "description": "A selector, or — in a terminal demo — `output` for the rows the last\ncommand wrote, `cursor` for the live prompt line, or `text=…` to\nframe whatever rows contain that text."
                                          },
                                          "level": {
                                            "type": "number",
                                            "minimum": 1,
                                            "maximum": 4,
                                            "description": "Magnification: 1 = whole viewport, 2 = half of it, …"
                                          },
                                          "ms": {
                                            "type": "integer",
                                            "minimum": 0,
                                            "description": "Camera move duration (ms)."
                                          }
                                        },
                                        "additionalProperties": false
                                      }
                                    ]
                                  }
                                },
                                "required": [
                                  "zoom"
                                ],
                                "additionalProperties": false,
                                "description": "Explicit camera control, for when auto-zoom's choice isn't the story you're\ntelling. \"out\" is a wide establishing shot."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "expect": {
                                    "type": "object",
                                    "properties": {
                                      "selector": {
                                        "type": "string",
                                        "minLength": 1
                                      },
                                      "text": {
                                        "type": "string",
                                        "description": "The element's text must contain this."
                                      },
                                      "count": {
                                        "type": "integer",
                                        "minimum": 0,
                                        "description": "Exactly this many elements must match."
                                      },
                                      "visible": {
                                        "type": "boolean",
                                        "default": true,
                                        "description": "Whether the element must be visible (default true)."
                                      }
                                    },
                                    "required": [
                                      "selector"
                                    ],
                                    "additionalProperties": false
                                  }
                                },
                                "required": [
                                  "expect"
                                ],
                                "additionalProperties": false,
                                "description": "An assertion. Unlike `waitFor` (which only proves an element appears), this\nchecks what the app actually rendered — so `reel check` is a real smoke\ntest, not just a selector-existence probe."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "hold": {
                                    "type": "integer",
                                    "minimum": 0
                                  }
                                },
                                "required": [
                                  "hold"
                                ],
                                "additionalProperties": false,
                                "description": "Explicit pause — discouraged, but sometimes you want a deliberate hold."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "run": {
                                    "anyOf": [
                                      {
                                        "type": "string"
                                      },
                                      {
                                        "type": "object",
                                        "properties": {
                                          "cmd": {
                                            "type": "string"
                                          },
                                          "input": {
                                            "type": "string",
                                            "description": "Text piped to stdin, for commands that prompt."
                                          },
                                          "expectCode": {
                                            "type": "integer",
                                            "description": "Fail the run unless the command exits with this code."
                                          },
                                          "replayMs": {
                                            "type": "integer",
                                            "minimum": 0,
                                            "description": "Override the replay budget for this command (ms)."
                                          },
                                          "hidden": {
                                            "type": "boolean",
                                            "default": false,
                                            "description": "Run the command off camera.\n\nFor the setup a demo needs but nobody wants to watch — seeding a\nfixture, creating a scratch directory, installing something. The\ncommand still runs and `expectCode` still applies, so it stays part of\nwhat `reel check` verifies; it simply never reaches the screen.\n\nNote that each command runs in its own shell, so `cd` in a hidden step\ndoes not carry into later ones. Set `terminal.cwd` for that."
                                          }
                                        },
                                        "required": [
                                          "cmd"
                                        ],
                                        "additionalProperties": false
                                      }
                                    ]
                                  }
                                },
                                "required": [
                                  "run"
                                ],
                                "additionalProperties": false,
                                "description": "Type a command at the prompt, run it, and replay its output. The command\nreally runs — so like `expect`, this makes `reel check` a genuine smoke\ntest of the CLI, not a scripted illusion of one."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "expectOutput": {
                                    "type": "string"
                                  }
                                },
                                "required": [
                                  "expectOutput"
                                ],
                                "additionalProperties": false,
                                "description": "Assert the terminal screen contains this text."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "clear": {
                                    "type": "boolean"
                                  }
                                },
                                "required": [
                                  "clear"
                                ],
                                "additionalProperties": false,
                                "description": "Clear the terminal screen."
                              },
                              {
                                "type": "object",
                                "properties": {
                                  "show": {
                                    "type": "string",
                                    "enum": [
                                      "terminal",
                                      "app"
                                    ]
                                  }
                                },
                                "required": [
                                  "show"
                                ],
                                "additionalProperties": false,
                                "description": "Switch between the terminal and the app underneath it."
                              }
                            ]
                          },
                          "minItems": 1
                        }
                      },
                      "required": [
                        "label",
                        "steps"
                      ],
                      "additionalProperties": false
                    },
                    "minItems": 2
                  }
                },
                "required": [
                  "paths"
                ],
                "additionalProperties": false
              }
            },
            "required": [
              "branch"
            ],
            "additionalProperties": false,
            "description": "A fork the viewer chooses between, in the interactive build."
          }
        ]
      },
      "minItems": 1,
      "description": "The demo itself, in order."
    },
    "output": {
      "type": "object",
      "properties": {
        "preset": {
          "type": "string",
          "enum": [
            "share",
            "readme",
            "hq",
            "social",
            "docs"
          ],
          "default": "share",
          "description": "Delivery profile; sets defaults for the fields below."
        },
        "gif": {
          "type": "string"
        },
        "mp4": {
          "type": "string"
        },
        "webm": {
          "type": "string"
        },
        "storyboard": {
          "type": "string",
          "description": "Directory to drop a PNG storyboard (one image per beat)."
        },
        "html": {
          "type": "string",
          "description": "Self-contained interactive HTML: a click-through of the same demo, with\nhotspots on the elements you acted on. One file, no hosting."
        },
        "fps": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 60,
          "description": "Frame rate for video (overrides preset)."
        },
        "maxWidth": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "description": "Cap the output width in px (overrides preset)."
        },
        "gifFps": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 50,
          "description": "GIF-specific overrides (GIFs trade size for smoothness/palette)."
        },
        "gifMaxWidth": {
          "type": "integer",
          "exclusiveMinimum": 0
        },
        "gifColors": {
          "type": "integer",
          "minimum": 16,
          "maximum": 256
        },
        "subtitles": {
          "type": [
            "boolean",
            "string"
          ],
          "description": "Emit sidecar subtitles from the captions. true, or an explicit path base."
        },
        "audio": {
          "type": "boolean",
          "description": "Mux the narration into the video. Needs an `audio:` block and at least one\n`say:` line; without either it is a no-op, not an error, so a spec can\ncarry the flag before it carries the script."
        },
        "audioTrack": {
          "type": "string",
          "description": "Also write the bare mixed track here, for editing elsewhere."
        },
        "languages": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Localize the subtitles into these languages, e.g. [\"es\",\"fr\"]."
        },
        "targetDuration": {
          "anyOf": [
            {
              "type": "number",
              "exclusiveMinimum": 0
            },
            {
              "type": "string"
            }
          ],
          "description": "Fit the finished demo to a length: `30`, `\"30s\"`, `\"1500ms\"`. Applied\nafter `polish.trimIdle`, by rescaling the recorded timeline rather than\nre-running the demo. Useful where length is a hard limit (social embeds)."
        }
      },
      "additionalProperties": false,
      "description": "What gets rendered, and where it is written."
    },
    "cuts": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "What this cut is for — `youtube`, `readme`. Used in logs and errors."
          },
          "from": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "number",
                "minimum": 0
              }
            ],
            "description": "Beat label or ms offset to start from. Omit to start at the beginning."
          },
          "to": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "number",
                "minimum": 0
              }
            ],
            "description": "Beat label or ms offset to end at. Omit to run to the end."
          },
          "output": {
            "type": "object",
            "properties": {
              "preset": {
                "type": "string",
                "enum": [
                  "share",
                  "readme",
                  "hq",
                  "social",
                  "docs"
                ],
                "default": "share",
                "description": "Delivery profile; sets defaults for the fields below."
              },
              "gif": {
                "type": "string"
              },
              "mp4": {
                "type": "string"
              },
              "webm": {
                "type": "string"
              },
              "storyboard": {
                "type": "string",
                "description": "Directory to drop a PNG storyboard (one image per beat)."
              },
              "html": {
                "type": "string",
                "description": "Self-contained interactive HTML: a click-through of the same demo, with\nhotspots on the elements you acted on. One file, no hosting."
              },
              "fps": {
                "type": "integer",
                "exclusiveMinimum": 0,
                "maximum": 60,
                "description": "Frame rate for video (overrides preset)."
              },
              "maxWidth": {
                "type": "integer",
                "exclusiveMinimum": 0,
                "description": "Cap the output width in px (overrides preset)."
              },
              "gifFps": {
                "type": "integer",
                "exclusiveMinimum": 0,
                "maximum": 50,
                "description": "GIF-specific overrides (GIFs trade size for smoothness/palette)."
              },
              "gifMaxWidth": {
                "type": "integer",
                "exclusiveMinimum": 0
              },
              "gifColors": {
                "type": "integer",
                "minimum": 16,
                "maximum": 256
              },
              "subtitles": {
                "type": [
                  "boolean",
                  "string"
                ],
                "description": "Emit sidecar subtitles from the captions. true, or an explicit path base."
              },
              "audio": {
                "type": "boolean",
                "description": "Mux the narration into the video. Needs an `audio:` block and at least one\n`say:` line; without either it is a no-op, not an error, so a spec can\ncarry the flag before it carries the script."
              },
              "audioTrack": {
                "type": "string",
                "description": "Also write the bare mixed track here, for editing elsewhere."
              },
              "languages": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "Localize the subtitles into these languages, e.g. [\"es\",\"fr\"]."
              },
              "targetDuration": {
                "anyOf": [
                  {
                    "type": "number",
                    "exclusiveMinimum": 0
                  },
                  {
                    "type": "string"
                  }
                ],
                "description": "Fit the finished demo to a length: `30`, `\"30s\"`, `\"1500ms\"`. Applied\nafter `polish.trimIdle`, by rescaling the recorded timeline rather than\nre-running the demo. Useful where length is a hard limit (social embeds)."
              }
            },
            "additionalProperties": false,
            "description": "What gets rendered, and where it is written."
          }
        },
        "required": [
          "name",
          "output"
        ],
        "additionalProperties": false
      },
      "description": "Shorter deliverables cut out of this same recording.\n\nOne demo usually has to be several: a long walkthrough for YouTube, a\nminute for LinkedIn, forty seconds for Twitter, something much shorter for\na README. Written as separate specs they drift apart within a month — the\ncaption gets fixed in one and not the others — which is the failure this\nwhole tool exists to prevent.\n\nA cut is not a second recording. It names a range of the demo that was\nalready filmed and re-encodes the frames already on disk, so it costs no\nbrowser time and cannot disagree with the master about what the app did."
    }
  },
  "required": [
    "steps",
    "output"
  ],
  "additionalProperties": false
}
